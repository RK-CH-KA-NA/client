// 'use strict';

// var app = app || {};

// (function(module) {

//   function Playlists(playlistData) {
//     Object.keys(playlistData).forEach(key => this[key] = playlistData[key]);
//   };

// Playlists.prototype.toHtml = function() {
//     return app.render('playlist-template', this);
//   };